import { Router } from "express";
import { ExerciseController } from "../controllers/ExerciseController.js";
const router = Router();
const exerciseController = new ExerciseController();
router.post("/create", exerciseController.createExercise);
router.get("/getAll", exerciseController.getAllExercise);
router.get("/get/:exerciseId", exerciseController.getExerciseById);
router.put("/update/:exerciseId", exerciseController.updateExercise);
router.delete("/delete/:exerciseId", exerciseController.DeleteExercise);
export default router;
